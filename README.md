React.js demo using randomuser.me API to generate random user profiles.

Work in progress so some final touches are missing.

To see it in action:

* clone the repo
* cd into it
* `npm install`
* `npm start`